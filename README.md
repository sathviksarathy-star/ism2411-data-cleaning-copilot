# ism2411-data-cleaning-copilot
A data cleaning tool which loads a messy sales dataset, cleans common issues like inconsistent column names, extra whitespace, missing values, and invalid numbers, and then saves a cleaned version
